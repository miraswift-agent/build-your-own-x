//! agent-browser library — Stage 01: HTML Parser
//!
//! Exposes the `html` module for use by integration tests and downstream crates.
//! The CLI binary (`src/main.rs`) provides the command-line interface.

pub mod html;
