//! agent-browser library — Stage 02: DOM Tree
//!
//! Exposes the `html` and `dom` modules for use by integration tests and downstream crates.
//! The CLI binary (`src/main.rs`) provides the command-line interface.

pub mod dom;
pub mod html;
