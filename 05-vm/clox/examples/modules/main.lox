// Stage 64 complete demo — whole-module + selective + rename
// Run: ../../bin/clox examples/modules/main.lox  (from clox/)

import "mathutil.lox" as math;
import { add as sum, PI } from "mathutil.lox";

print math.add(1, 2);
print sum(10, 5);
print PI;
print typeof(math);
